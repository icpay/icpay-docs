export const metadata = {
  title: 'SDK Install',
  description: 'Install the ICPay SDK using your preferred package manager.',
}

export const sections = [
  { title: 'Install', id: 'install' },
]

# SDK Install

## Install

### If you're using pnpm
```bash
pnpm add @icpay/sdk
```

### If you're using npm
```bash
npm install @icpay/sdk
```

### If you're using yarn
```bash
yarn add @icpay/sdk
```


